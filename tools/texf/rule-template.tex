\documentclass[12pt,varwidth]{standalone}

% The book style assumes a chaptered document class.
% standalone.cls uses article.cls, so we provide minimal chapter/part counters.
\makeatletter
\@ifundefined{c@chapter}{\newcounter{chapter}}{}
\@ifundefined{thechapter}{\newcommand{\thechapter}{\arabic{chapter}}}{}
\makeatother

% Pull the book's exact LaTeX macros for symbols, metavariables, and Fitch proofs.
\usepackage{forallxyyc}

% In the book's quick reference, some rules use \mathem to align proof lines.
\newlength{\mathemwd}
\settowidth{\mathemwd}{\mbox{\small m}}
\def\mathem{{\hbox to\mathemwd{}}}

\begin{document}
\setlength{\parindent}{0pt}

$body$
\end{document}
